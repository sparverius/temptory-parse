val xs = (if foo then 0)
val xs = (if foo then true else false)

////

fun loop1(x0:x0,yy:!yy): bool =
(
case+yy of
  | list0_vt_nil()=>true
  | list0_vt_cons(y0,yy)=>
  (
    if test
    then loop1(x0,yy)
    else false
  )
  where
  {
    val test = gseq_x2forall$test<px><py><xs,x0><ys,y0>(x0,y0)
  }
)
